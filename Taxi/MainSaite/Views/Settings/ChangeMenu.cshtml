@model Model.DTO.UserDTO

@{
    ViewBag.Title = @Resources.Resource.ChangeMenu;
}
@Html.ValidationSummary()
<div class="btform">
    @using (Html.BeginForm("ChangeMenu"))
    {
        <table class="table">

            @Html.Partial("_EditUser", Model)
        </table>

        <input type="submit" name="Ok" value="Ok" />
    }
</div>